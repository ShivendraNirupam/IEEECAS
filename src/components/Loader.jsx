const Loader = () => {
  return (
    <div className="h-screen w-screen flex items-center justify-center bg-black text-white">
      <div className="text-xl animate-pulse">Loading IEEE CAS...</div>
    </div>
  );
};

export default Loader;
